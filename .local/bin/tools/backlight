#!/usr/bin/env bash
# AUTHOR: Bram Pulles

# Increase the brightness of the screen with the specified amount in $1.
readonly DEVICE=/sys/class/backlight/intel_backlight/brightness
readonly OLD=$(cat $DEVICE)
readonly NEW=$((OLD + $1))
echo $NEW > $DEVICE 2> /dev/null
