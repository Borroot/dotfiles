#!/usr/bin/env bash
# AUTHOR: Bram Pulles

# Show a notification when the battery is low.
while true
do
	battery_info=$(acpi -b)
	if [[ $(echo $battery_info | grep Discharging) && $(echo $battery_info | cut -f 5 -d " ") < 00:10:00 ]]
	then
		 notify-send -u critical -t 10000 "Low Battery!" "$battery_info"
	fi
	sleep 10
done
